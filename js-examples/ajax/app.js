/* ajax */

/*
        1. ajax = making request behind the scenes.
        2. a = asynchronous, j = javascript, a = and, x = xml/json.
        3. you can use JSON.parse(JSON object) to convert JSON to a javascript object.
        4. you can use hoppscotch or postman to mimic HTTP requests without creating a frontend.
*/

/* http verbs/status codes */

/*
        1. GET = receive data, POST = save data on server, DELETE
        2. 200 = OK, 400 = error
*/
